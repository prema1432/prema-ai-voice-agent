fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
eval_type_backport; python_version < "3.10"
motor>=3.4
httpx>=0.27
python-multipart>=0.0.9
numpy>=1.24

# ── Voice engines (optional, install for real STT/TTS/VAD) ──────────────────
# faster-whisper>=1.0        # STT_BACKEND=whisper
# piper-tts>=1.2             # TTS_BACKEND=piper
# torch>=2.2 --index-url https://download.pytorch.org/whl/cpu   # silero VAD / indic models
# transformers>=4.40         # TTS_BACKEND=indic_parler / STT_BACKEND=indicconformer

# ── Dev / test ───────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
