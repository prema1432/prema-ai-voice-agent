[general]
static = yes

; All AI-driven channels enter Stasis; the backend controls them via ARI.
[default]
exten => _X.,1,Stasis(freebuff-voice)

; Inbound calls from the SIP trunk also land in the AI app.
[from-trunk]
exten => _X.,1,Stasis(freebuff-voice)
same => n,Hangup()
